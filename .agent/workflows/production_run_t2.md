---
description: T2 No-IDsモデルを用いた本番予測の実行手順
---

# 競馬AI 本番運用ガイド (2026年版)

## クイックスタート

### 当日のレース予測
```bash
docker compose exec app python scripts/production_run_t2_jit.py
```

### 特定日付の予測
```bash
docker compose exec app python scripts/production_run_t2_jit.py --date 20260105
```

### Discord通知付きで実行
```bash
docker compose exec app python scripts/production_run_t2_jit.py --discord
```

---

## 使用モデル

| 項目 | 値 |
|------|-----|
| モデルパス | `models/experiments/exp_t2_no_ids/model.pkl` |
| 特徴量数 | 111 |
| ID特徴量 | なし（汎化性重視） |
| Valid AUC | 0.7142 |

### モデルの特徴
- ✅ パフォーマンスベース予測（`mean_rank_5`, `lag1_rank` 等）
- ✅ 新馬・未知馬への汎化性向上
- ✅ 解釈性の向上

---

## 自動実行設定

### JITスケジューラ（レース10分前に自動予測）
```bash
# フォアグラウンド実行
docker compose exec app python scripts/jit_scheduler.py

# バックグラウンド実行（推奨）
docker compose exec -d app python scripts/jit_scheduler.py
```

### Discord Webhook設定
`.env` ファイルに設定:
```bash
DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_TOKEN"
```

---

## 出力例

```text
================================================================================
 🏇 KEIIBA-AI JIT PREDICTIONS (T2 No-IDs Model)
 Date: 2026-01-05
================================================================================

[中山 11R] (ID: 202606010511)
No  馬名                騎手          予測確率    オッズ
---------------------------------------------------------------
 7  ブレイディヴェーグ    ルメール       15.2%      2.8
 2  サンプル馬           北村友一        8.5%     12.3
...

💡 推奨: 単勝7番 (EV > 1.0)
```

---

## 注意事項

1. **データ要件**: JRA-VANデータが最新であること
2. **PC稼働**: スケジューラはPC起動中のみ動作
3. **Docker**: `docker compose up -d app` でコンテナ起動必須

---

## トラブルシューティング

### モデルが見つからない
```bash
ls models/experiments/exp_t2_no_ids/
```

### 特徴量生成エラー
```bash
# キャッシュクリア
rm -rf data/jit_features_temp/
```

### DB接続エラー
```bash
docker compose restart db
```
